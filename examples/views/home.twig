<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }} - {{ appName }}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css">
</head>
<body class="bg-gray-100 text-gray-800 font-sans">

  <div class="max-w-3xl mx-auto mt-10 bg-white shadow-lg rounded-xl p-6">
    <h1 class="text-3xl font-bold mb-2">{{ title }}</h1>
    <p class="text-gray-600 mb-4">Hello, <strong>{{ user.name }}</strong> ({{ user.role }})</p>

    <h2 class="text-xl font-semibold mb-2">Notifications:</h2>
    <ul class="list-disc list-inside text-gray-700 mb-4">
      {% for note in notifications %}
        <li>{{ note }}</li>
      {% else %}
        <li>No new notifications.</li>
      {% endfor %}
    </ul>

    {{ view('components/footer')|raw }}
  </div>

</body>
</html>
